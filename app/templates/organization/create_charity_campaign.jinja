{% extends "base.jinja" %}

{% block title %}Create Charity Campaign{% endblock %}

{% block content %}
<div class="container mt-2">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1>{{ _("Create a New Campaign") }}</h1>
            <form method="post">
                <fieldset>
                    <div class="form-group">
                        <label for="name">{{ _("Name:") }}</label>
                        <input class="form-control" type="text" id="name" name="name" required><br>
                        <label for="description">{{ _("Description:") }}</label>
                        <textarea class="form-control" id="description" name="description" rows="3"
                            required></textarea><br>
                        <label for="authority_id">{{ _("Authority:") }}</label>
                        <select class="form-control" id="authority_id" name="authority_id" required>
                            {% for authority in authorities %}
                            <option value="{{ authority.id }}">{{ authority.name }}</option>
                            {% endfor %}
                        </select><br>
                        <button type="submit" class="btn btn-primary">{{ _("Create Campaign") }}</button>
                    </div>
                </fieldset>
            </form>
        </div>
    </div>
</div>
{% endblock %}